---
phase: 01-search-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - src/lib/utils/competition.test.ts
  - src/hooks/useSearch.test.ts
  - src/components/KeywordCard.test.ts
  - src/app/api/malt/autocomplete/route.test.ts
  - tests/e2e/search.spec.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "Test framework is installed and configured to run TypeScript tests via npm run test"
    - "Test files exist with stubs for all Phase 1 requirements (Wave 0 gate completion)"
    - "Test stubs reference the implementation files they will verify (no stubs testing stubs)"
    - "E2E test infrastructure is ready to run live Vercel deployment tests"
  artifacts:
    - path: vitest.config.ts
      provides: "Vitest + TypeScript + jsdom configuration for unit and component testing"
      contains: "defineConfig, jsdom"
    - path: src/lib/utils/competition.test.ts
      provides: "Test stubs for competition color logic (SRCH-04)"
      contains: "describe, it, expect"
    - path: src/hooks/useSearch.test.ts
      provides: "Test stubs for debounce + SWR + dedup behavior (SRCH-01)"
      contains: "describe, it, expect"
    - path: src/components/KeywordCard.test.ts
      provides: "Test stubs for volume display and color rendering (SRCH-02, SRCH-04)"
      contains: "describe, it, render"
    - path: src/app/api/malt/autocomplete/route.test.ts
      provides: "Test stubs for API proxy, validation, caching (INFRA-01, INFRA-02)"
      contains: "describe, it, expect"
    - path: tests/e2e/search.spec.ts
      provides: "Test stubs for live Vercel deployment smoke test (INFRA-03)"
      contains: "test, expect"
  key_links:
    - from: "vitest.config.ts"
      to: "all test files"
      via: "import configuration"
      pattern: "vitest.config"
    - from: "all test files"
      to: "implementation files"
      via: "import and test"
      pattern: "from.*src"
---

<objective>
Install test infrastructure and create test stubs for all Phase 1 requirements. This is a Wave 0 setup task that gates all subsequent implementation waves.

Purpose: Establish test-first foundation so that Waves 1 and 2 can run tests after each commit and verify feature completion immediately.

Output:

- Vitest configuration with TypeScript + jsdom
- Test stub files for 4 unit/component tests + 1 integration test + 1 E2E test
- npm run test and npm run test:watch commands ready to execute
  </objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-search-foundation/01-RESEARCH.md
@.planning/phases/01-search-foundation/01-UI-SPEC.md
@.planning/phases/01-search-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Vitest with TypeScript + jsdom</name>
  <files>vitest.config.ts, package.json</files>
  <action>
Install Vitest 1.0+, @vitest/ui, and jsdom as dev dependencies. Create vitest.config.ts with:
- jsdom environment for component/DOM testing
- TypeScript preset
- test files glob pattern: **/*.test.ts, **/*.test.tsx
- coverage setup (optional for v0 but helpful)
- alias for @ → src/

Update package.json scripts:

- "test": "vitest run"
- "test:watch": "vitest watch"
- "test:e2e": "playwright test" (placeholder for tests/e2e/search.spec.ts)

Per VALIDATION.md: vitest.config.ts is required as Wave 0 dependency. No implementation code needed — only test infrastructure.
</action>
<verify>
<automated>npm list vitest @vitest/ui jsdom | grep -E "vitest|jsdom"</automated>
</verify>
<done>vitest.config.ts exists, npm run test and npm run test:watch commands work without errors, TypeScript is recognized in test files</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for competition color logic (SRCH-04)</name>
  <files>src/lib/utils/competition.test.ts</files>
  <action>
Create unit test file with stub tests for getCompetitionLevel() and getCompetitionColor().

Stub structure (RED phase — tests fail until implementation exists):

- describe('getCompetitionLevel', () => {
  it('returns "rare" for volume < 10', () => {})
  it('returns "common" for volume 10-100', () => {})
  it('returns "oversaturated" for volume > 100', () => {})
  })
- describe('getCompetitionColor', () => {
  it('returns green badge styles for "rare"', () => {})
  it('returns amber badge styles for "common"', () => {})
  it('returns red badge styles for "oversaturated"', () => {})
  })

Per VALIDATION.md task 1-01-02: These tests verify SRCH-04 (color-coded competition signal). Thresholds: rare <10, common 10-100, oversaturated >100 (from UI-SPEC.md). Colors: rare=#10B981 (green), common=#F59E0B (amber), oversaturated=#EF4444 (red).

Stubs should be empty (no expect() assertions) or have placeholder expect(true).toBe(true). Wave 1 task will write actual implementation.
</action>
<verify>
<automated>npm run test -- src/lib/utils/competition.test.ts 2>&1 | grep -E "PASS|FAIL|describe|it"</automated>
</verify>
<done>Test file exists and is parseable, test structure matches VALIDATION.md mapping, file imports would fail (implementation doesn't exist yet) or passes with stubbed assertions</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for useSearch hook (SRCH-01)</name>
  <files>src/hooks/useSearch.test.ts</files>
  <action>
Create test file for useSearch() custom hook that manages debounce + SWR + dedup.

Stub test structure (tests for Wave 1 implementation):

- describe('useSearch', () => {
  it('debounces input by 300ms before fetching', () => {})
  it('deduplicates requests within 300ms window (SWR)', () => {})
  it('returns loading state while fetching', () => {})
  it('returns results after successful fetch', () => {})
  it('returns error state on API failure', () => {})
  })

Per VALIDATION.md task 1-01-03: Tests verify SRCH-01 (live debounced search). Must test:

- Debounce delay (300ms per UI-SPEC.md)
- Deduplication: rapid typing doesn't fire multiple requests
- State management: query, debouncedQuery, results, isLoading, error

Mock SWR and fetch to avoid real API calls. Use fake timers (vitest.useFakeTimers()) for debounce testing.

Stubs: empty or placeholder expect(true).toBe(true).
</action>
<verify>
<automated>npm run test -- src/hooks/useSearch.test.ts 2>&1 | grep -E "PASS|FAIL|describe|it"</automated>
</verify>
<done>Test file exists, useSearch import would be attempted, test structure covers debounce + dedup + state, placeholder assertions in place</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for KeywordCard component (SRCH-02, SRCH-04)</name>
  <files>src/components/KeywordCard.test.ts</files>
  <action>
Create component test file for KeywordCard. This component displays: keyword label + volume count ("N utilisateurs Malt") + competition badge (color-coded per SRCH-04).

Stub test structure:

- describe('KeywordCard', () => {
  it('renders keyword label', () => {})
  it('displays volume count with "utilisateurs Malt" label', () => {})
  it('applies green badge styles for rare competition', () => {})
  it('applies amber badge styles for common competition', () => {})
  it('applies red badge styles for oversaturated competition', () => {})
  })

Per VALIDATION.md task 1-01-04: Tests verify SRCH-02 (volume display) and SRCH-04 (color-coded badge). Use vitest + @testing-library/react for rendering.

Per UI-SPEC.md:

- Component layout: flex row with keyword | volume | badge
- Volume label format: "{N} utilisateurs Malt"
- Competition colors: rare #10B981, common #F59E0B, oversaturated #EF4444
- Background: secondary (gray-100) or white, padding 16px

Stubs: empty test bodies or placeholder expect(true).toBe(true).
</action>
<verify>
<automated>npm run test -- src/components/KeywordCard.test.ts 2>&1 | grep -E "PASS|FAIL|describe|it"</automated>
</verify>
<done>Test file exists, component imports attempted, test structure covers rendering + volume + colors, placeholder assertions in place</done>
</task>

<task type="auto">
  <name>Task 5: Create test stubs for API proxy route (INFRA-01, INFRA-02, SRCH-03)</name>
  <files>src/app/api/malt/autocomplete/route.test.ts</files>
  <action>
Create integration test file for the API proxy Route Handler at GET /api/malt/autocomplete.

Stub test structure:

- describe('GET /api/malt/autocomplete', () => {
  it('validates query parameter (min 2 chars)', () => {})
  it('forwards query to Malt API endpoint', () => {})
  it('parses Malt response with Zod schema', () => {})
  it('returns 5-10 related suggestions alongside primary term', () => {})
  it('includes volume count in response', () => {})
  it('sets Cache-Control header: max-age=0, s-maxage=60, stale-while-revalidate=300', () => {})
  it('handles Malt API timeout (>5s) gracefully', () => {})
  it('returns user-friendly error message on API failure', () => {})
  })

Per VALIDATION.md tasks 1-01-01, 1-02-01, 1-02-02:

- INFRA-01: Proxy, validation, error handling
- INFRA-02: Cache-Control headers
- SRCH-03: 5-10 related suggestions with volume

Mock fetch to simulate Malt API responses. Test both success and error paths.

Malt API endpoint: https://www.malt.fr/profile/public-api/suggest/tags/autocomplete?query={query}
Expected response shape (from RESEARCH.md): { suggestions: [{ label: string, volume?: number }] }

Per RESEARCH.md: Set upstream timeout to 5s, return user-friendly message on error. Cache header: max-age=0, s-maxage=60, stale-while-revalidate=300

Stubs: empty test bodies.
</action>
<verify>
<automated>npm run test -- src/app/api/malt/autocomplete/route.test.ts 2>&1 | grep -E "PASS|FAIL|describe|it"</automated>
</verify>
<done>Test file exists, Route Handler imports attempted, test structure covers validation + Malt call + response parsing + cache headers + errors, placeholder assertions in place</done>
</task>

<task type="auto">
  <name>Task 6: Create E2E test stubs for Vercel deployment (INFRA-03)</name>
  <files>tests/e2e/search.spec.ts</files>
  <action>
Create E2E test file using Playwright (or Cypress — per RESEARCH.md, Playwright is standard for Next.js/Vercel projects).

Stub test structure:

- test('Search page loads on Vercel', async ({ page }) => {})
- test('Can type keyword and see results in <1 second', async ({ page }) => {})
- test('Cache header observable on second search of same term', async ({ page }) => {})
- test('Error message displays if Malt API fails', async ({ page }) => {})

Per VALIDATION.md task 1-03-01: Tests verify INFRA-03 (Vercel deployment, <1s response). These are manual-only for now (require live Vercel URL), but infrastructure is required.

Playwright config: tests/playwright.config.ts (separate from Vitest). Stubs can point to localhost:3000 initially; execute phase will update to live Vercel URL.

Stubs: empty test bodies with .skip() or placeholder expect(true).toBe(true).
</action>
<verify>
<automated>test -f tests/e2e/search.spec.ts && grep -E "test|describe" tests/e2e/search.spec.ts | head -5</automated>
</verify>
<done>E2E test file exists, test structure in place, Playwright imports configured (or skipped), ready for Wave 2 to enable and run against deployed URL</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run test:watch` — all test files load without import errors (stubs may fail but don't crash)
2. Run `npm run test` — full suite runs in <60s (likely <30s for stubs), produces PASS/FAIL output for each test file
3. Verify test files reference correct implementation paths (no circular imports, correct @/ aliases)
4. Confirm Wave 0 completion checklist (from VALIDATION.md):
   - [ ] vitest.config.ts exists
   - [ ] All 6 test files exist (competition.test.ts, useSearch.test.ts, KeywordCard.test.ts, route.test.ts, search.spec.ts)
   - [ ] Test suite runs without import errors
   - [ ] `npm run test` and `npm run test:watch` work as expected
</verification>

<success_criteria>

- Vitest is installed and configured
- All test stub files created and parsed successfully
- npm run test runs without crashing (individual stubs may fail, but test runner is operational)
- Test files follow structure in VALIDATION.md and reference implementation paths from RESEARCH.md architecture
- Nyquist Rule: Every test file can be run and produces output (PASS/FAIL/PENDING)
- Wave 0 gate complete: subsequent waves can commit implementation code and immediately run matching tests
  </success_criteria>

<output>
After completion, create `.planning/phases/01-search-foundation/01-01-SUMMARY.md` with:
- Test infrastructure installed and ready
- Test file paths and structure documented
- Stub test counts per file
- Next steps: Wave 1 implementation begins, tests transition from RED → GREEN
</output>
