---
phase: 04-landing-page-brand
plan: 01
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/app/globals.css
  - src/app/page.tsx
  - src/components/Hero.tsx
  - src/components/CTAButton.tsx
autonomous: true
requirements: [LAND-01]

must_haves:
  truths:
    - "Landing page renders hero section with headline and subheadline above fold"
    - "Search input is immediately visible after hero section"
    - "CTA button below hero scrolls to search input on click"
    - "Hero section is responsive on mobile and desktop"
  artifacts:
    - path: "src/components/Hero.tsx"
      provides: "Hero headline + subheadline component"
      min_lines: 40
    - path: "src/components/CTAButton.tsx"
      provides: "Scroll-to-search CTA button"
      min_lines: 35
    - path: "src/app/globals.css"
      provides: "fadeInUp and fadeOut CSS animations"
      contains: "@keyframes fadeInUp"
    - path: "src/app/page.tsx"
      provides: "Page structure with Hero section above search"
      exports: ["Hero", "CTAButton", "SearchInput"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/Hero.tsx"
      via: "import and render"
      pattern: "<Hero />"
    - from: "src/app/page.tsx"
      to: "src/components/CTAButton.tsx"
      via: "pass searchInputRef"
      pattern: "<CTAButton searchInputRef={searchInputRef}"
    - from: "src/components/CTAButton.tsx"
      to: "HTMLInputElement"
      via: "useRef + forwardRef"
      pattern: "searchInputRef.current?.scrollIntoView"
---

<objective>
Foundation wave: Set up test infrastructure, create animations, build hero section and primary CTA, integrate into page structure.

Purpose: Establish responsive landing page layout with hero messaging above fold and smooth scroll-to-search interaction before leaderboard and FAQ are added.

Output: Hero component with headline/subheadline, CTAButton with scroll behavior, updated page.tsx with all three sections stacked (Hero → CTA → SearchInput), animations ready for Wave 2 leaderboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-landing-page-brand/04-CONTEXT.md
@.planning/phases/04-landing-page-brand/04-RESEARCH.md
@.planning/phases/04-landing-page-brand/04-UI-SPEC.md
@.planning/phases/04-landing-page-brand/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fadeInUp and fadeOut CSS animations to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Append custom CSS keyframes to src/app/globals.css for leaderboard stagger animation and post-verification success state fade. Do NOT remove existing content. Per UI-SPEC.md animations section:

    Add these keyframes at end of file before any @layer directives:

    ```css
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(8px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeOut {
      from {
        opacity: 1;
      }
      to {
        opacity: 0;
      }
    }
    ```

    Then add Tailwind @layer utilities:

    ```css
    @layer utilities {
      .animate-fade-in {
        animation: fadeInUp 300ms ease-out forwards;
      }

      .animate-fade-out {
        animation: fadeOut 300ms ease-out forwards;
      }
    }
    ```

    These animations will be applied in Wave 2 (leaderboard stagger) and Wave 3 (success state). Verify syntax by checking file parses without CSS errors.

  </action>
  <verify>
    <automated>grep -c "@keyframes fadeInUp" src/app/globals.css && grep -c "@keyframes fadeOut" src/app/globals.css && grep -c "\.animate-fade-in" src/app/globals.css && echo "✓ All animations added"</automated>
  </verify>
  <done>globals.css contains fadeInUp, fadeOut keyframes and .animate-fade-in, .animate-fade-out utility classes</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero component (headline + subheadline)</name>
  <files>src/components/Hero.tsx</files>
  <action>
    Create src/components/Hero.tsx with headline, subheadline, and optional primary CTA below hero text. Per UI-SPEC.md Hero Section and CONTEXT.md D-01 (hero above fold, before leaderboard and search):

    Component structure:
    - Container: max-w-2xl mx-auto, centered
    - Padding: px-4 py-12 on mobile, px-6 lg:px-8 on desktop
    - Background: inherits page gradient (no custom bg needed)
    - Headline: text-3xl sm:text-4xl, font-bold, text-gray-900, centered, mb-4
    - Subheadline: text-base, font-normal, text-gray-600, centered, mb-8
    - Apply Harry Dry copy framework (D-03): headline must be concrete, visualizable, falsifiable, unique to Malt keyword searching

    Example headline (executor may refine per copy tests):
    "See which keywords are actually searched on Malt — find your competitive edge"

    Example subheadline (executor may refine):
    "Type any skill and instantly see how many Malt users claim it. Discover the keywords that will make your profile stand out."

    Include optional small CTA button (secondary, labeled "Try it free") below subheadline before search section. Button styling: bg-indigo-100 text-indigo-700, not the primary indigo button (that's the main CTA between leaderboard and search).

    Component should be "use client" (client-side component). No props needed initially.

  </action>
  <verify>
    <automated>grep -c "export function Hero" src/components/Hero.tsx && grep -c "text-3xl sm:text-4xl" src/components/Hero.tsx && grep -c "text-gray-900" src/components/Hero.tsx && echo "✓ Hero component created"</automated>
  </verify>
  <done>Hero.tsx exists, exports Hero component, renders headline (28-32px responsive), subheadline (16px), and container is centered with proper spacing per UI-SPEC.md</done>
</task>

<task type="auto">
  <name>Task 3: Create CTAButton component (scroll-to-search)</name>
  <files>src/components/CTAButton.tsx</files>
  <action>
    Create src/components/CTAButton.tsx with scroll-to-search behavior. Per CONTEXT.md D-11 (click CTA before email gate → scroll nudge, does NOT trigger email form):

    Component structure:
    - "use client" component
    - Accepts searchInputRef (React.RefObject<HTMLInputElement>) as prop
    - Button styling per UI-SPEC.md CTA Button:
      - bg-indigo-600 text-white, py-3 px-6, rounded-lg
      - hover:bg-indigo-700, focus:ring-2 focus:ring-indigo-600
      - Full width on mobile, auto width on desktop (center alignment in page layout)
    - Label text: Apply Harry Dry framework (D-10) — concrete, visualizable, falsifiable, unique
      Example labels (executor refines): "Find My Rare Keywords", "See My Competition", "Search My Niche"
      Avoid generic "Sign Up Free", "Get Started"
    - On click:
      - Call searchInputRef.current?.scrollIntoView({ behavior: "smooth", block: "center" })
      - Focus the input: searchInputRef.current?.focus()
      - Optional: briefly show toast/inline message "Try it — search any skill" (can be simple console.log for now, real notification comes in later phase)
    - Disabled state styling (opacity-50, cursor-not-allowed) should be applied but button starts enabled

    This CTA appears below hero (primary) and between leaderboard and search (secondary) and above FAQ (tertiary) — all instances use same component.

  </action>
  <verify>
    <automated>grep -c "export function CTAButton" src/components/CTAButton.tsx && grep -c "scrollIntoView" src/components/CTAButton.tsx && grep -c "bg-indigo-600" src/components/CTAButton.tsx && echo "✓ CTAButton component created"</automated>
  </verify>
  <done>CTAButton.tsx exists, exports CTAButton component accepting searchInputRef prop, implements smooth scroll to search input on click, styled with indigo accent per UI-SPEC.md</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Hero and CTAButton into page.tsx, establish page sequence</name>
  <files>src/app/page.tsx</files>
  <action>
    Modify src/app/page.tsx to establish landing page structure per CONTEXT.md D-07 (sequence locked: Hero → Leaderboard → CTA → Search → Results → FAQ):

    Current page structure (Phase 1-2 complete):
    - SearchInput component
    - ResultsList (conditionally shown)
    - EmailGate (conditionally shown after 3 searches)
    - useSearch hook managing state

    New page structure (after Wave 1):
    - Hero component (at top, above fold)
    - [Wave 2 will add: Leaderboard component here]
    - CTAButton component (scroll to search, between leaderboard and search)
    - SearchInput component (existing, with ref passed from page)
    - ResultsList (existing, conditionally shown)
    - EmailGate (existing)
    - [Wave 3 will add: FAQ component and SuccessState]

    Integration steps:
    1. Import Hero and CTAButton at top of page.tsx
    2. Create searchInputRef using useRef<HTMLInputElement>(null)
    3. Render Hero before all other content
    4. Render CTAButton, passing searchInputRef prop: <CTAButton searchInputRef={searchInputRef} />
    5. Pass ref to SearchInput component: <SearchInput ref={searchInputRef} /> (SearchInput must be forwardRef)
    6. Keep existing search, results, and email gate logic unchanged

    Container structure (use existing max-w-2xl mx-auto, min-h-screen bg-gradient-to-br):
    ```
    <main className="max-w-2xl mx-auto min-h-screen bg-gradient-to-br from-white to-gray-50">
      <Hero />
      {/* Wave 2 adds Leaderboard here */}
      <CTAButton searchInputRef={searchInputRef} />
      <SearchInput ref={searchInputRef} {...searchProps} />
      {/* Results and gate */}
      {/* Wave 3 adds FAQ and SuccessState here */}
    </main>
    ```

    Do NOT modify existing useSearch hook or email gate logic. This task is integration only.

  </action>
  <verify>
    <automated>grep -c "import { Hero }" src/app/page.tsx && grep -c "import { CTAButton }" src/app/page.tsx && grep -c "<Hero />" src/app/page.tsx && grep -c "searchInputRef" src/app/page.tsx && grep -c "<CTAButton" src/app/page.tsx && echo "✓ Page.tsx integrated"</automated>
  </verify>
  <done>page.tsx imports Hero and CTAButton, creates searchInputRef, renders components in correct sequence (Hero → CTA → SearchInput → Results), ref is passed to CTAButton and SearchInput</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Update SearchInput component to accept forwardRef</name>
  <files>src/components/SearchInput.tsx</files>
  <action>
    Verify or update src/components/SearchInput.tsx to accept forwardRef so that page.tsx can pass the searchInputRef to it. SearchInput should forward ref to underlying input element.

    Current pattern (likely):
    ```typescript
    export function SearchInput({ value, onChange, disabled }: SearchInputProps) {
      return <input ... />
    }
    ```

    Required pattern:
    ```typescript
    export const SearchInput = React.forwardRef<HTMLInputElement, SearchInputProps>(
      ({ value, onChange, disabled }, ref) => (
        <input ref={ref} ... />
      )
    );
    SearchInput.displayName = "SearchInput";
    ```

    This is a small, focused change. If SearchInput already uses forwardRef, no changes needed. If not, update to support ref forwarding. No behavior changes, only adding ref support.

  </action>
  <verify>
    <automated>grep -c "React.forwardRef" src/components/SearchInput.tsx && grep -c "SearchInput.displayName" src/components/SearchInput.tsx && echo "✓ SearchInput supports forwardRef"</automated>
  </verify>
  <done>SearchInput component accepts and forwards ref to underlying input element; ref can be used by parent (page.tsx) for scroll and focus interactions</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm test` to verify test stubs parse (no errors even if tests are empty)
2. Run `npm run build` to verify Hero and CTAButton components compile
3. Visually inspect rendered page at localhost:3000 to confirm:
   - Hero headline and subheadline visible at top
   - Search box visible below hero without scrolling
   - CTA button appears between hero and search
   - Hero section is responsive on mobile (375px) and desktop (1440px)
4. Manual verification: Click CTA button, verify smooth scroll to search input
</verification>

<success_criteria>

- [x] globals.css contains fadeInUp and fadeOut animations
- [x] Hero component renders headline (28-32px), subheadline (16px), centered
- [x] CTAButton component renders with indigo styling and scroll behavior on click
- [x] page.tsx imports and renders Hero and CTAButton in correct sequence
- [x] SearchInput accepts forwardRef from page.tsx
- [x] No errors on `npm run build`
- [x] Page loads at localhost:3000 with Hero visible above fold
- [x] Clicking CTA button smoothly scrolls to search input
- [x] Page is responsive (no horizontal scrolling) on mobile 375px viewport
      </success_criteria>

<output>
After completion, create `.planning/phases/04-landing-page-brand/04-01-SUMMARY.md` documenting:
- Test stubs created (3 files)
- Animations added to globals.css
- Hero component (headline, subheadline, centered layout)
- CTAButton component (scroll behavior, indigo styling)
- page.tsx integration (correct sequence, ref forwarding)
- Any discretionary copy decisions made (headline, subheadline, CTA label per Harry Dry framework)
</output>
